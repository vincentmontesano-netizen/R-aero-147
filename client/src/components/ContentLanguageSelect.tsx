import {useI18n} from '@/i18n';
export type ContentLanguage='fr'|'en'|'ar';
export default function ContentLanguageSelect({value,onChange,generation=false,disabled=false}:{value:ContentLanguage;onChange:(value:ContentLanguage)=>void;generation?:boolean;disabled?:boolean}){
 const {lang}=useI18n();const label=generation?(lang==='fr'?'Langue IA : texte, audio et QCM':lang==='ar'?'لغة النص والصوت والاختبارات المولدة':'AI language: text, audio and quizzes'):(lang==='fr'?'Langue de la formation':lang==='ar'?'لغة الدورة':'Course language');
 return <label className="block text-xs font-medium space-y-1"><span>{label}</span><select disabled={disabled} value={value} onChange={e=>onChange(e.target.value as ContentLanguage)} className="w-full h-9 rounded-md border px-2 text-sm"><option value="fr">Français</option><option value="en">English</option><option value="ar">العربية</option></select>{generation&&<span className="block text-xs font-normal text-muted-foreground">{lang==='fr'?'Ce choix s’applique aux nouvelles générations ; il ne traduit pas le contenu existant.':lang==='ar'?'ينطبق هذا الاختيار على التوليد الجديد ولا يترجم المحتوى الحالي.':'This choice applies to new generations; it does not translate existing content.'}</span>}</label>;
}
