# Landing R-AERO — 23 septembre 2026

Implémentation de la demande jointe `pasted-text-1.txt` dans la page d’accueil existante, sans modifier les fichiers Claude, les mémoires ou les README.

## Parcours livré

- Header orienté indépendants, Part-145/MRO, CAMO et compagnies ; CTA inscription et brochure.
- Étude de turbine Three.js chargée séparément ; pause, préférence de mouvement réduit, arrêt hors champ, image de repli sans WebGL et nettoyage des ressources.
- Trois repères fonctionnels vérifiables : une interface, trois formats, un code de vérification. Aucun taux de réussite ou nombre d’alumni inventé.
- Problème/solution, quatre bénéfices, témoignage explicitement illustratif et quatre FAQ de conversion. Les FAQ administrables restent accessibles dans un volet complémentaire.
- Formulaire avec nom, e-mail, profil, organisation facultative et accord de contact. Réutilise `quotes.create` avec la mention « Brochure R-AERO », le profil, la langue et une clé d’idempotence. Les demandes apparaissent dans le suivi commercial existant. Aucun abonnement marketing ajouté.
- Confirmation après persistance, référence et téléchargement via `public.cataloguePdf` ; reprise des erreurs sans recréer le lead.
- Français, anglais et arabe ; dispositions responsive et RTL conservées. Brochure existante en français.

## Vérification

- `pnpm check` : succès.
- Build via le script `sites-building` : succès (avertissements de taille de bundles existant/prévisible pour Three.js ; chunk 3D séparé).
- `server/quoteRequestInput.test.ts` et `server/quoteCreation.integration.test.ts` : 3 tests réussis, dont persistance et concurrence/idempotence, sur une base temporaire supprimée après contrôle. Aucune notification commerciale de test envoyée.
- Accueil et ressources d’entrée servis en HTTP 200 sur `http://localhost:3175/`.
- API catalogue et téléchargement : PDF valide, 5412 octets sur les données locales. En-tête corrigé contrôlé via rendu Poppler.
- Pas de test visuel/interactif navigateur : outil CUA indisponible (`CUA_REPL_ENABLED_SURFACES is required`).

## Points éditoriaux

La FAQ existante indique un agrément Part-147 en cours. L’en-tête du générateur PDF a été aligné sur cette mention, déjà présente dans son pied de page. La page ne prétend pas que l’agrément est acquis.

Consigne utilisateur actualisée : aucun prix ni comparatif tarifaire sur la landing page. Les cartes de formations affichent « Voir le programme » ; la section des offres tarifaires et la promesse de budget divisé par deux ont été retirées. La FAQ porte sur le démarrage du parcours. Les prix restent dans le catalogue et les parcours commerciaux existants. Remplacer le témoignage illustratif par un avis autorisé lorsque disponible.

## Livraison locale

Sources et sortie compilée mises à jour ; aperçu Docker existant `raero-local-preview` actualisé, données conservées. Pas de publication distante : l’application Express/PostgreSQL existante conserve son architecture et son hébergement. Ne pas publier uniquement son frontend statique sur Sites, car le formulaire, les comptes et le catalogue nécessitent son API.

La sortie locale est aussi présente dans `dist/`. Une reconstruction future de l’image Docker doit utiliser les sources actuelles pour conserver ces modifications.
